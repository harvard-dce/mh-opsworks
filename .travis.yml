before_install:
  - "echo '--colour' > ~/.rspec"
  - "echo 'gem: --no-document' > ~/.gemrc"
before_script:
cache:
  - bundler
language:
  - ruby
notifications:
  email:
    - false
rvm:
  - 2.0.0
  - 2.2.10
  - 2.4.5
sudo: false
script: bundle exec rspec spec/
